node_modules
dist
.env
.env.local
.firebase
*.log
*.tsbuildinfo

# Firebase CLI local agent tooling (machine-local, not site code)
.agents/
.claude/skills/
skills-lock.json

# Service account keys are real credentials, unlike the web config.
# Never commit one.
*serviceAccount*.json
*-firebase-adminsdk-*.json
