# URL of the deployed Cloud Run function that receives qualify-form submissions.
# Leave empty during local design work; the form runs in prototype mode and does not send data.
VITE_QUALIFY_ENDPOINT=
